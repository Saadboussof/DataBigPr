 
TaaSim · Casablanca Urban Mobility 
ENSA Al Hoceima — For Academic Use 
Page  
 RMSE and MAE on test set. Must be compared against naive 7-day-lag baseline. 
Feature importance chart required: Step 4 — Evaluation 
 model.save('s3a://ml/models/demand_v1/') to MinIO: Step 5 — Save artifact 
 FastAPI loads model at startup: POST /api/demand/forecast → {zone_id, datetime} → 
{predicted_demand}: Step 6 — Serve via API 
 
🎓  Evaluation Requirement 
The ML model is only considered successful if it achieves lower RMSE than the naive 7-day-lag 
baseline. 
Students must include a table comparing model vs baseline per zone in their technical report. 
Feature importance chart must explain the top 3 predictors — what drives demand in 
Casablanca? 
 
 
